{{#if multiple}}
export { default as Control } from './{{pascalCase name}}Control'
export { default as Preview } from './{{pascalCase name}}Preview'
{{else}}
export { default } from './{{pascalCase name}}Control'
{{/if}}
