backend:
  name: test-repo
  login: false
media_folder: assets
collections:
- name: example
  label: Example
  files:
  - file: _data/{{dashCase name}}.yml
    name: {{dashCase name}}-widget
    label: {{titleCase name}} Widget
    fields:
    - name: {{dashCase name}}
      label: {{titleCase name}}
      widget: {{dashCase name}}
